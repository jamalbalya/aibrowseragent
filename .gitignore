node_modules/
dist/
# A second bundle built only for the E2E auth fixture. Never shipped.
dist-auth/
# The shipped bundle with `downloads` required instead of optional, built
# only for the E2E downloads fixture. Never shipped.
dist-downloads/
coverage/
*.zip
# Build output. The digest is recorded in docs/testing/release/README.md
# together with the toolchain it was measured on, because that is what makes
# it meaningful; a bare .sha256 committed here would read as authoritative
# for any build, which is exactly what is not being claimed.
/release/
.DS_Store
.env
.env.*
!.env.example
*.log
.vite/

test-results/
playwright-report/
blob-report/
.playwright/
