dist
# The E2E fixture bundles. Emitted, not authored — and `dist-downloads/` is
# asserted byte-identical to `dist/`, so reformatting it would break the very
# guarantee it exists to provide.
dist-auth
dist-downloads
coverage
node_modules
package-lock.json

# The authoritative specification is committed verbatim. Formatting it would
# modify the document of record.
docs/spec/

# Generated from the installed licences by scripts/generate-notices.mjs, and
# compared byte-for-byte by `npm run check:notices`. Reformatting it would put
# two gates in conflict: prettier would rewrite the file and the notices check
# would then report it as drifted from the licences it was generated from.
THIRD-PARTY-NOTICES.md
